#include <iostream>
#include <cstring>

int main (int argc, char** argv)
{
	char buffer[512];
	std::strcpy(buffer, argv[1]);

	printf("blue or red pill ?");
	char buffer2[512];
	std::cin >> buffer2; 
	system("cmatrix");

	return 0;
}
